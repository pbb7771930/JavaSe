实现通信的步骤：
1、常见连个NIO线程组，一个专门用于网络事件处理（接受客户端的连接），另一个则进行网络通信读写。
2、创建一个ServerBootstrap对象，配置Netty的一系列参数
3、创建一个实际处理数据的类ChannelInitializer，进行初始化的准备工作，比如设置接收传出数据的字符集、格式、已经实际处理数据的接口。
4、绑定端口，执行同步阻塞方法等待服务器端启动即可











